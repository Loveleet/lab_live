# Copy to secrets.env and fill in. Do NOT commit secrets.env to git.
# All API keys, DB credentials, and secrets go here. Server loads this at startup.

# --- Database (use either DATABASE_URL or DB_*)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# DB_HOST=150.241.244.130
# DB_PORT=5432
# DB_USER=lab
# DB_PASSWORD=your_db_password
# DB_NAME=olab

# --- Server
# PORT=10000
# NODE_ENV=production

# --- Python signals API (Node proxies to this for /api/open-position, /api/calculate-signals)
# PYTHON_SIGNALS_URL=http://localhost:5001

# --- Optional
# ENABLE_SELF_PING=true
# VERBOSE_LOG=true
# SYNC_POSITIONS_TIMEOUT_MS=180000
# CALCULATE_SIGNALS_TIMEOUT_MS=300000

# Action password (Auto-Pilot, Execute, End Trade) is in DB: lab_settings table, key action_password.
# Add allowed frontend origins in server.js allowedOrigins if you use a custom domain.
