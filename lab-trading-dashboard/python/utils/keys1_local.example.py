# Copy this file to keys1_local.py and set your Binance API key and secret.
# keys1_local.py is gitignored — it will not be committed.
# Used as fallback when BINANCE_API_KEY / BINANCE_SECRET are not set (e.g. for open-position, sync-open-positions).

api = "your_binance_api_key_here"
secret = "your_binance_secret_here"
