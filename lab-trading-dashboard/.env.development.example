# Optional: use this when running locally (npm run dev) to point at the cloud API.
# Copy to .env.local and set the current tunnel URL if your laptop can't reach the cloud IP.
# Get current URL: curl -s http://150.241.244.130:10000/api/tunnel-url
# Or run: npm run use-cloud-api  (fetches URL and writes .env.local)

VITE_API_BASE_URL=https://your-tunnel.trycloudflare.com
