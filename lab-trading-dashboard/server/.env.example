# Optional: use cloud or remote PostgreSQL (no local DB needed)
# Real trading data = olab (same as Python Final_olab_database.py). Demo/seed data = labdb2
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Or use DB_* (default when unset: host=150.241.244.130, database=olab)
# DB_HOST=150.241.244.130
# DB_NAME=olab

# For local PostgreSQL (macOS often uses your username, not "postgres"):
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=apple
# DB_PASSWORD=
# DB_NAME=labdb2

# PORT=10000
